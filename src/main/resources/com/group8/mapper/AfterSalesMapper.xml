<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group8.mapper.AfterSalesMapper">
    <resultMap id="AfterSalesResultMap" type="com.group8.entity.AfterSales">
        <id property="id" column="id" />
        <result property="orderId" column="order_id" />
        <result property="buyerId" column="buyer_id" />
        <result property="sellerId" column="seller_id" />
        <result property="afterSalesType" column="after_sales_type" />
        <result property="status" column="status" />
        <result property="reason" column="reason" />
        <result property="refundAmount" column="refund_amount" />
        <result property="reviewComment" column="review_comment" />
    </resultMap>
    
    <select id="getById" resultMap="AfterSalesResultMap">
        SELECT 
            id, order_id, buyer_id, seller_id, after_sales_type, status, 
            reason, refund_amount, review_comment
        FROM after_sales
        WHERE id = #{id}
    </select>
    
    <select id="list" resultMap="AfterSalesResultMap">
        SELECT 
            id, order_id, buyer_id, seller_id, after_sales_type, status, 
            reason, refund_amount, review_comment
        FROM after_sales
        <where>
            <if test="params.buyerId != null">
                AND buyer_id = #{params.buyerId}
            </if>
            <if test="params.sellerId != null">
                AND seller_id = #{params.sellerId}
            </if>
            <if test="params.status != null">
                AND status = #{params.status}
            </if>
            <if test="params.afterSalesType != null">
                AND after_sales_type = #{params.afterSalesType}
            </if>
        </where>
        ORDER BY id DESC
        <if test="params.offset != null and params.limit != null">
            LIMIT #{params.offset}, #{params.limit}
        </if>
    </select>
    
    <select id="count" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM after_sales
        <where>
            <if test="params.buyerId != null">
                AND buyer_id = #{params.buyerId}
            </if>
            <if test="params.sellerId != null">
                AND seller_id = #{params.sellerId}
            </if>
            <if test="params.status != null">
                AND status = #{params.status}
            </if>
            <if test="params.afterSalesType != null">
                AND after_sales_type = #{params.afterSalesType}
            </if>
        </where>
    </select>
    
    <insert id="create" parameterType="com.group8.entity.AfterSales" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO after_sales (
            order_id, buyer_id, seller_id, after_sales_type, status, 
            reason, refund_amount
        ) VALUES (
            #{orderId}, #{buyerId}, #{sellerId}, #{afterSalesType}, #{status}, 
            #{reason}, #{refundAmount}
        )
    </insert>
    
    <update id="update" parameterType="com.group8.entity.AfterSales">
        UPDATE after_sales
        SET 
            status = #{status}, 
            review_comment = #{reviewComment}
        WHERE id = #{id}
    </update>
    
    <update id="updateStatus">
        UPDATE after_sales
        SET status = #{status}
        WHERE id = #{id}
    </update>
    
    <select id="getPendingList" resultMap="AfterSalesResultMap">
        SELECT 
            id, order_id, buyer_id, seller_id, after_sales_type, status, 
            reason, refund_amount, review_comment
        FROM after_sales
        <where>
            <if test="params.sellerId != null">
                AND seller_id = #{params.sellerId}
            </if>
            <if test="params.afterSalesType != null">
                AND after_sales_type = #{params.afterSalesType}
            </if>
            AND status = 1
        </where>
        ORDER BY id DESC
        <if test="params.offset != null and params.limit != null">
            LIMIT #{params.offset}, #{params.limit}
        </if>
    </select>
    
    <select id="countPending" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM after_sales
        <where>
            <if test="params.sellerId != null">
                AND seller_id = #{params.sellerId}
            </if>
            <if test="params.afterSalesType != null">
                AND after_sales_type = #{params.afterSalesType}
            </if>
            AND status = 1
        </where>
    </select>
</mapper>
